[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mavis-agent"
version = "0.8.0"
description = "Mavis is a voice-first agent harness built on Hermes Agent with local-first speech-to-speech defaults."
